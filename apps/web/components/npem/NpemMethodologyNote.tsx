export function NpemMethodologyNote() {
  return (
    <section className="panel">
      <h3>Methodology Note</h3>
      <p>N.P.E.M. separates group selection from scoring. Core archetypes are preferred for headline rankings, while overlays are labelled as overlapping cohorts.</p>
      <p>All component scores are normalized from 0 to 100 with winsorized percentile scaling. Burden metrics are inverted so higher scores always mean stronger economic position.</p>
    </section>
  );
}
